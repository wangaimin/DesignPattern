状态模式：允许对象在内部状态改变时改变他的行为，对象看起来好像修改了他的类

设计总结：和策略模式很像

本例：糖果机售卖有许多状态。先投币2元>转动曲柄>售出糖果>糖果数目等于0>糖果售罄  糖果数目大于0可继续投币以此循环。判断状态会用到很多if else 。这里用状态模式可以解决很多问题，也能封装变化。
      需求变化：加入了幸运之星，转动曲柄时10%概率获得两个糖果，此时只需要扩展一种状态就行


优点：封装了变化的部分，当有需求变更或加入新状态时，影响不大。
缺点：多创造了较多的类


.NET中的应用：


JAVA中的应用：
