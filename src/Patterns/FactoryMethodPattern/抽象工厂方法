抽象工厂方法：提供一个创建一系列相关或相互依赖的接口，而无须指定他们的具体类

设计总结：

本例：上海餐馆和四川餐馆由不同食材工厂提供食材

.NET中的应用：
              System.Data.Common.DbProviderFactory，这个类位于System.Data.dll程序集中,该类扮演抽象工厂模式中抽象工厂的角色
              DbProviderFactory类是一个抽象工厂类，该类提供了创建数据库连接时所需要的对象集合的接口，实际创建的工作在其子类工厂中进行，微软使用的是SQL Server数据库，因此提供了连接SQL Server数据的具体工厂实现

JAVA中的应用：
