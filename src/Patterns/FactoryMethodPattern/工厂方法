工厂方法模式：定义了一个创建对象的接口，但由子类来决定要实例化的类是哪一个。工厂方法把类的实例化推迟到子类当中。

设计总结：要依赖抽象，不要依赖具体类。

本例：需要制作回锅肉，各个地方制作回锅肉的顺序是相同的，都是先煮再炒，但是配料不同，煮、炒时间也不同。定义了回锅肉父类，父类中指定了制作顺序，不同回锅肉必须按照这个顺序制作。
      在子类中获取具体回锅肉实例，实例中实现不同煮方法和时间已经配料。

.NET中的应用：
              Asp.net中，处理程序对象是具体用来处理请求，当我们请求一个*.aspx的文件时，此时会映射到System.Web.UI.PageHandlerFactory类上进行处理，
              而对*.ashx的请求将映射到System.Web.UI.SimpleHandlerFactory类中（这两个类都是继承于IHttpHandlerFactory接口的）

JAVA中的应用：
