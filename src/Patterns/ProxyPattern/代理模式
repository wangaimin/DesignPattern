代理模式：为另一个对象提供一个替身或占位符，以控制这个对象的访问。

设计总结：被代理的对象可以是远程对象、创建开销大的对象或需要安全控制的对象。

本例：  比如jsonRPC起到访问远程对象的作用，jsonRPC起到代理作用，有了它就可以访问远程对象，让访问远程对象像访问本地对象一样简单。
        一些序列化相关的操作也不需要管，只单纯调用就行，一切都有代理操作。


优点：
缺点：


.NET中的应用：


JAVA中的应用：
