单例模式：确保一个类只有一个实例，并提供一个全局的访问点。

设计总结：用枚举实现单例是一个很全面的方案，由JVM从根本上提供保障，绝对防止多次实例化，是更简洁、高效、安全的实现单例的方式。
         （实际和饿汉式一个原理，枚举是创建静常量，类加载的时候就创建。）


本例：


.NET中的应用：


JAVA中的应用：在Jdk中搜索SingleTon，比较多，但是不知道干啥的，就不记下来了。以后明白了再写
